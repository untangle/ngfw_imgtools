# This image is based on the regular hd-media image, and gets all the
# packages from that one too.

# Remove things other than USB and MMC to make it fit
ata-modules-${kernel:Version} -
sata-modules-${kernel:Version} -
pata-modules-${kernel:Version} -
scsi-modules-${kernel:Version} -
scsi-common-modules-${kernel:Version} -
scsi-extra-modules-${kernel:Version} -
virtio-modules-${kernel:Version} -
serial-modules-${kernel:Version} -
usb-serial-modules-${kernel:Version} -
md-modules-${kernel:Version} -
lvm2-udeb -

# usb-storage-modules depends on scsi-core-modules
# isofs-modules depends on cdrom-core-modules
