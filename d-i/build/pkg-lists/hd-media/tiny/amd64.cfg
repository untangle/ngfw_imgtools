# This image is based on the regular hd-media image, and gets all the
# packages from that one too. And the removals in common.cfg.

brltty-udeb -
libasound2-udeb -
screen-udeb -
fb-modules-${kernel:Version} -
input-modules-${kernel:Version} -
ext4-modules-${kernel:Version} -
